{
  "name": "kuruma-logger-support-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vitepress dev site --host",
    "build": "vitepress build site",
    "preview": "vitepress preview site"
  },
  "devDependencies": {
    "vitepress": "^1.6.4"
  }
}
